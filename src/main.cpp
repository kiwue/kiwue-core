#include <iostream>

using namespace std;

const char* application = "kiwue-server";
const char* version = "0.0.1";

int main() {
    std::cout << application << " " << version << std::endl;
    return 0;
}
